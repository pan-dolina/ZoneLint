# govulncheck configuration for ZoneLint
# https://pkg.go.dev/golang.org/x/v

# Only fail on high/critical severity vulnerabilities.
severity: high

# Ignore known-acceptable findings in test-only code paths.
ignore:
  # miekg/dns test helpers are not compiled into the binary.
